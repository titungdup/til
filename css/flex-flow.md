A Flexbox related shorthand property. It specifies the direction of a flex container and its wrapping behavior i.e `flex-direction` and `flex-wrap`.

Example:

`flex-flow: row wrap;`

`flex-flow: column wrap-reverse;`
